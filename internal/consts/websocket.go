package consts

// Customer service notification types shared with Java and uniapp.
const (
	KefuMessageType             = "kefu_message_type"
	KefuMessageReadStatusChange = "kefu_message_read_status_change"
)
