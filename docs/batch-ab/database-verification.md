# T02 / T03 verification report

Verified locally on 2026-09-07, PostgreSQL 16.10 Homebrew, aarch64 Darwin, Go toolchain from the parent environment baseline. Isolated instance: loopback 55439, database ruoyi_ab; integration tests create and drop independent schemas. Commands use GOCACHE=/private/tmp/ruoyi-ab-go-build, GOPATH=/private/tmp/ruoyi-ab-gopath and TEST_POSTGRES_DSN as documented in database-runbook.md. All referenced logs are under docs/batch-ab/evidence.

The six SQL migration files are frozen, with hashes in `migrations.sha256`; all six hashes verified. The migration executor is golang-migrate v4.18.3. The last deployed isolated database version is controlled by the parent smoke run; this agent's restore acceptance explicitly confirms version 6, dirty=false (database-restore-v6.log).

| Task / case | Status | Actual command / test | Result and evidence | Limits |
|---|---|---|---|---|
| T02 / A01 empty database | Passed | `rtk proxy go test ./migrations -count=1 -v`, TestPostgresMigrationCoverageAndRepeat | Creates baseline from empty schema; all 120 table models have exactly their effective source columns and actual GORM projections compile. database-final-tests.log | Schema coverage is not all service query behavior. |
| T02 repeat migration | Passed | Same suite, TestPostgresMigrationCoverageAndRepeat | Reapply latest produces no new menus/data; 6 component-backed pages exist; no pay channel is seeded. database-final-tests.log | No real merchant channel. |
| T02 existing baseline upgrade | Passed | TestPostgresBaselineUpgradePreservesData | Starts at migration 1, inserts member data, upgrades to latest, confirms preserved data and seed sequences advancing beyond explicit IDs. database-final-tests.log | Synthetic upgrade, not an unexamined legacy production dataset. |
| T02 failed upgrade | Passed | TestPostgresFailedUpgradeStaysDirtyAndReleasesConnection | Deliberate conflicting seed fails with 23505, seed transaction rolls back, version remains 3/dirty, next migration refuses, pool remains usable. database-final-tests.log | Operators must inspect and repair; no automatic force/down. |
| T02 executor resource lifecycle | Reproduced then passed | TestPostgresMigrationReleasesCallerConnection | Before fix InUse=1, expected0: database-connection-red.log. After WithConnection+deferred release, MaxOpenConns(1), repeat migration and Ping pass. database-final-tests.log | Separate adapter wrapper; core executor remains pinned upstream. |
| T02 / A28 backup and restore component | Passed | `rtk proxy python3 scripts/postgres_restore_check.py` | PostgreSQL custom dump restored into another new database. All121 tables including migration state match counts/ordered full-row MD5; all sequence values match; synthetic bcrypt user and 3,000,000,001-fen SKU preserved; migration at6 clean and reapply unchanged. database-restore-v6.log | A28 full order-to-after-sale/Redis/external side effects are not covered. Archive is synthetic and temporary. |
| T02 administrator / hostname | Passed | `rtk proxy go test ./cmd/bootstrap ./pkg/config -count=1 -v` | Explicit credentials bcrypt-verify; role association and tenant hostname persist; duplicate bootstrap refuses reset; malformed hostname and weak JWT checks pass. Offline platform_admin is absent by default, granted only for a new explicit opt-in user, and malformed opt-in values are rejected. bootstrap-config.log and database-final-tests.log | Parent owns actual HTTP host resolution, login, cookies/tokens and browser acceptance. |
| T03 real type reads/writes | Passed | TestPostgresTypeRoundTripsZeroUpdateAndSoftDelete | Actual models: JSON/CSV lists, payment client JSON, offset timestamp, time-of-day, large integer fen, default true/false, explicit true, zero numeric updates, soft-delete read/update behavior. database-final-tests.log | Not every optional custom type and service arithmetic path. Native bool remains bool. |
| T03 uniqueness / transaction / lock | Passed | TestPostgresConstraintsRollbackUpsertAndLock | Duplicate tenant/mobile rejected; same mobile other tenant accepted; ordinary soft-deleted identifier reusable; injected rollback preserves stock; PostgreSQL ON CONFLICT update works without security plugin; NOWAIT competing row lock returns55P03. database-final-tests.log | Runtime tenant plugin intentionally restricts general upsert; accepted database syntax is not blanket application authorization. |
| T03 product statistics | Passed | `rtk proxy go test ./internal/repo/product -count=1 -v` | Trusted tenant required; foreign tenant and soft-deleted browse rows excluded; paid sums preserve3,000,000,001 fen; repeated day creates exactly one live statistic. statistics-postgres.log and database-final-tests.log | Paid order IDs currently held in memory per tenant, bind batches1000; concurrent retry may report unique conflict. |
| T03 service BitBool updates | Passed | `rtk proxy go test ./internal/service/infra ./internal/service/mall/product ./internal/service/mall/promotion ./internal/service/mall/trade/brokerage ./internal/service/mall/trade -count=1` | Real file master switch writes true/false, old master clears, other tenant unchanged; touched service packages compile/existing tests pass. database-service-bindings.log | Promotion package has no tests; its corrected bind literals are compile/static verified only. T04 assertions belong to its report. |
| T02 source registry drift | Passed | `rtk proxy go test ./internal/schema -count=1 -v` | Source AST TableName receiver inventory matches every registry entry. schema-registry.log | Legacy structs lacking TableName/current references explicitly excluded in manifest. |
| T02 / T03 immutable SQL integrity | Passed | `rtk proxy shasum -a 256 -c docs/batch-ab/migrations.sha256` | All six hashes OK. migrations-checksums.log | golang-migrate does not persist historical content hashes; review/CI must enforce immutable files. |
| T03 MySQL SQL audit | Partial / bounded scope | Source scan in evidence/data-access-audit.txt and data-baseline.md | Core statistics IFNULL/Raw replaced; member FIND_IN_SET fixed by transaction agent. Optional IoT DATE_FORMAT/JSON_CONTAINS, brokerage alias queries and unsupported member-mail email query recorded. | These optional paths are not reported compatible. Parent owns authoritative enabled-module decisions. |
| T01 startup / HTTP | Parent-reported pass | Parent full server on :58091 | Parent reports login, permissions/pages, tenant denial, refresh revocation/logout passed and product smoke underway. Consult parent HTTP evidence. | This report does not substitute for parent T01/T09/T10 or whole-suite report. |
| T05+ real merchant configuration | External blocker | Not run | No real credentials/channel supplied, no real payment/refund attempted. | Disabled mall app and absent channel are deliberate explicit provisioning prerequisites. |

Primary implementation: pkg/database driver setup and parameterized SQL logging; pkg/config PostgreSQL/secret environment support; pkg/types BitBool dialect/default bindings; cmd/schema and internal/schema registry; migrations/*.up.sql and migrations.Apply; cmd/migrate; cmd/bootstrap; internal/testutil; product statistics and audited BitBool service bindings. Source-model tenant changes are coordinated parent/security work, incorporated into the SQL/manifest. DAO generation and full build/test ownership remain with the parent.

No commit was created. No original frontend source was modified by this task. Full production, real-channel and all A01-A28 acceptance are not claimed. See data-baseline.md for source decisions, seed inventory and remaining field/query limits; database-runbook.md for exact initialization and recovery procedures.

## Trade RBAC seed follow-up

Migration 6 (`000006_trade_permissions.up.sql`) adds all 38 new RequirePermission strings from internal/api/router/trade.go and associates each with tenant1 role1 only when that role is the live tenant_admin. It does not create or grant platform_admin. Migrations1..5 retain their exact original SHA256 values. No live schema was reset.

Real PostgreSQL `TestPostgresTradePermissionsUpgrade` starts at5, upgrades to6, reads the complete non-IoT router source union, verifies every required permission has exactly one live tenant1/role1 association, verifies no platform_admin role was seeded, and verifies reapplication does not add associations. The full migrations package passes in evidence/database-v6-tests.log. A new backup/restore rehearsal verifies version6 and all121 tables plus sequences in evidence/database-restore-v6.log. Historical version5 logs are retained as historical evidence, not relabeled.

The final router-union audit excludes the existing out-of-scope IoT permissions and the reserved service-authorized platform inspection endpoint. No extra role besides bootstrap tenant1 tenant_admin receives migration6 grants. The manifest includes refreshed query/service/router source hashes for this freeze.

Epicurus subsequently confirmed the trade permission strings are frozen, with no further changes planned. The security agent reports real PostgreSQL/Redis no-grant403 coverage for all normal registered trade routes. Authorization uses live HasPermission checks, so applying migration6 grant rows does not require a restart/policy reload; startup Casbin validation remains. This is security-agent evidence and its detailed runtime assertions belong to the security report. No additional platform permission seeds are required.
