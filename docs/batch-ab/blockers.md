# 剩余范围与阻塞清单

执行边界为任务书2.1的批次A、B。以下事项不能由A/B通过推断完成。

| 事项 | 状态 | 后续动作 / 输入 |
|---|---|---|
| T01完整前端接口适配、全页面端到端 | 待处理（D） | 契约盘点已有216调用点；18处方法/路径缺口，支付displayContent、sync语义等见CSV。保持原版页面，在Go侧逐项修复并验收 |
| T05 微信渠道、openid、终端签名参数 | 待处理（C）；真实渠道验收外部阻塞 | 先完成代码/模拟验收，再需要授权使用的商户号、AppID/OpenID、HTTPS回调域名、证书/公钥与API v3密钥。秘密通过安全配置渠道提供 |
| T06 通知DTO、JSON成功判定、幂等、重启恢复 | 待处理（C） | 当前通知任务租户/生命周期已修复；原有空DTO和SUCCESS文本协议未冒充完成。补可靠通知和接收端契约 |
| T07 支付/取消状态机、sync、关闭远程支付 | 待处理（C） | 本次只验收本地下单及未付款取消资源一次性恢复；未付款取消后本地支付记录仍待支付，必须完成关闭/对账后才能启用真实渠道 |
| T08 退款/售后资金闭环 | 待处理（C）；真实退款验收外部阻塞 | 金额分摊、并发退款、迟到回调和履约库存策略需独立测试；本批次无真实退款 |
| T11/T13 外部服务、CI/部署/监控 | 待处理（D） | 提供真实所需服务配置与上线目标后，完成真实接口和运维验收。当前构建/迁移/恢复手册可复现，不等于获准发布 |
| 平台跨租户写管理、部门/自定义/本人数据权限 | 不适用（首期明确未启用） | 平台只提供显式授权审计的只读租户汇总；部门范围拒绝配置且受限角色拒绝访问。若启用需新设计和独立验收，不能取消隔离拦截 |
| 全局ORM拦截之外的新Raw/直接database/sql或未验收旁支 | 待处理（随模块启用） | 不安全ORM形式当前拒绝；它不是数据库RLS，新增代码不得绕过边界。IoT、营销、分销等非首期模块不能凭表存在宣称功能可用 |
| 网络重试在已提交但响应丢失后重复创建订单 | 待处理（T12/D） | 本地失败可回滚并重试；尚无客户端请求幂等键，不能宣称所有重复提交已去重 |
| 所有账号密码变更后的全会话撤销 | 待处理（后续安全增强） | 当前登出撤销本会话、刷新轮换、禁用账号即时拒绝；不宣称密码重置撤销所有已签发会话 |
| 邮件模板测试发送HTTP绑定 | 待处理（T11/D） | MailHandler.SendMail原接口未绑定请求体；本次只验收MailService租户查询隔离，没有发送真实邮件或认证完整邮件功能 |

批次A/B工程验收无需用户额外提供商户秘密。进入真实支付/短信/邮件或生产阶段，需对应真实配置和明确操作授权；当前没有进行这些操作，也没有推送远程分支。
