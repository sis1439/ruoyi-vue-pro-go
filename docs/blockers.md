# C/D 当前边界与待提供项

本分支已经整合 A/B `6548c9e`。此前“无迁移、只支持 MySQL、无 PostgreSQL/Redis、缺认证租户、CI 集成空跑”的说明已过时。

- 真实微信/支付宝终端联调尚未执行。需要商户配置和测试回调域名；本次只使用合成签名、SDK、本地 HTTP 服务与隔离 PostgreSQL/Redis，未发起真实交易。
- 已取消订单迟到付款进入持久失败通知及人工对账/退款流程；不自动退款。通知重试耗尽已有数据库状态和错误日志，外部告警接入未完成。
- 历史 `merchant_order_id` 若使用展示单号，仍需业务数据盘点后设计迁移；没有对未知业务数据执行转换。新订单使用交易主键。
- 微信其他终端及支付宝终端未做真实验收；`wx_bar` 与支付宝转账明确不支持。
- 对象存储、真实短信、物流服务、生产备份及生产部署未执行；A/B 的隔离数据库备份恢复证据见 `docs/batch-ab/`。
- CI 配置及本地等价命令会验证真实依赖且拒绝跳过；没有推送分支，因此没有声称 GitHub Actions 已运行。

review 修复与测试结果见 `docs/batch-cd/verification-report.md`。A/B 的功能与范围以 `docs/batch-ab/verification-report.md` 为准，不能把本地测试通过等同于全部渠道生产验收。
